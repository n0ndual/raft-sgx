#ifndef Sgx_unsupported_h
#define Sgx_unsupported_h

int printf(const char *fmt, ...);

int sprintf(char* buf, const char *fmt, ...);

int asprintf(char** str, const char* fmt, ...);

int rand(void);

#endif
